/**
 * Render class.
 */
export class Camera {
  characters: CharacterSceneObject[];

  constructor() {
    // Init camera
  }

  render(ctx: CanvasRenderingContext2D) {
    // Camera render step
  }
}
